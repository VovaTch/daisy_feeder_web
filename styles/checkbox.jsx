import { StyleSheet } from "react-native";

export const checkboxStyles = StyleSheet.create({
  checkbox: {
    alignSelf: "center",
  },
  checkboxLabel: {
    color: "white",
    marginLeft: 8,
  },
  checkboxContainer: {
    flexDirection: "row",
    alignItems: "center",
    marginBottom: 15,
  },
});
